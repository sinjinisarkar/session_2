#include <stdio.h>
int main() 
{
    int a;
    int r;
        for (a = 1; a<11; a++)
        {
            r = a*a;
            printf("Square of: %d is %d\n", a, r);
            
        }
    return 0;
}